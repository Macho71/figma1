import 'package:flutter/material.dart';


class Downloand extends StatelessWidget {
  const Downloand({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text("Downloand")),
    );
  }
}